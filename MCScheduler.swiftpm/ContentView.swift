import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            CircleImage()
            
            VStack {
                Text("My Scheduler APP")
                /*
                 Add app name
                 new page with:
                    - TextField
                    - SaveButton
                    - Ignore/cancel button
                 */
                }
            }
            .padding()
        }
    }
}
