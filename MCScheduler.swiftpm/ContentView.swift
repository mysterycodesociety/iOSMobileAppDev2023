import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            CircleImage()
            
            VStack {
                Text("MCS Scheduler App")
                /*
                Add App Name
                 New Page with
                 - TextField
                 - SaveButton
                 - Ignore/Cancel button
                 */
                }
            }
            .padding()
        }
    }
}
